paths:
  raw_dir: "src/data/raw"
  cleaned_dir: "src/data/cleaned"
  chunks_dir: "src/data/chunks"
  vectorstore_dir: "src/vectorstore"
  logs_dir: "src/logs"

models:
  embedding_model: "BAAI/bge-small-en-v1.5"

chunking:
  chunk_size: 600
  chunk_overlap: 100
  min_chunk_chars: 80

retrieval:
  top_k: 5
  index_type: "flat_ip"

metadata:
  default_tags: ["enterprise", "rag", "day1"]